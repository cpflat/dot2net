name: deploy_and_virtual
topology:
  nodes:
    edge1:
      kind: linux
      image: nicolaka/netshoot
      network-mode: none
      binds:
      - edge1/etc/net/net.conf:/etc/net/net.conf
      exec:
      - ip link set lo up
    edge2:
      kind: linux
      image: nicolaka/netshoot
      network-mode: none
      binds:
      - edge2/etc/net/net.conf:/etc/net/net.conf
      exec:
      - ip link set lo up
    lan:
      kind: bridge
    probe:
      kind: linux
      image: nicolaka/netshoot
      network-mode: none


  links:
  - endpoints: [edge1:eth0, lan:eth0]
  - endpoints: [edge1:eth1, probe:eth0]
  - endpoints: [edge2:eth0, lan:eth1]
